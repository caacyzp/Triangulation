# Dependencies

GLM: https://github.com/g-truc/glm 

GLM is used to represent the vertex position of the given polygon, you could always implement your own math library. No specific GLM algebera opration is used.

# Caution
For the linked list part, the index based deletion might not work as people normally expected. Be careful on that part.
